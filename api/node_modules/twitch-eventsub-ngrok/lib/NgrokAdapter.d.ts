import { ConnectionAdapter } from 'twitch-eventsub';
/**
 * A connection adapter that uses ngrok to make local testing easy.
 */
export declare class NgrokAdapter extends ConnectionAdapter {
    private _listenerPortPromise?;
    private _hostNamePromise?;
    /** @protected */
    get connectUsingSsl(): boolean;
    /** @protected */
    getListenerPort(): Promise<number>;
    /** @protected */
    getExternalPort(): Promise<number>;
    /** @protected */
    getHostName(): Promise<string>;
}
